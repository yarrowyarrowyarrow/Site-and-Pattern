# Plans

One file per increment, written before the work and kept with it.

A plan here is not a status report — it is the **reasoning** behind a release:
what was actually wrong, what was measured, what was decided and why, and what
was deliberately left alone. The commit log says what changed; this says what we
believed at the time. When a later session asks "why is it like this?", the
answer should be findable without reconstructing it from diffs.

## Naming

```
docs/plans/V<major>.<minor>-<short-slug>.md
```

Version first, matching the release-branch convention in
[`CLAUDE.md`](../../CLAUDE.md), so plans sort in release order and a plan is
obviously paired with the branch that carried it out.

## Writing one

- **Lead with the evidence.** Numbers where there are numbers. "moist_mixedgrass
  is on 246 plants and aspen_parkland on 136" is worth more than "the ecoregion
  tags look wrong".
- **Say what you could not verify**, and why. A constraint recorded once saves
  the next session rediscovering it.
- **Record corrections in place.** When investigation falsifies part of the
  plan, amend it and say so — a plan that only ever describes what turned out to
  be true is a plan nobody can learn from.
- **Name what is not being done**, so the omission reads as a decision rather
  than an oversight.

## The plans

| Branch | Plan | What it set out to do | How it went |
|---|---|---|---|
| V2.73 | [a-second-week-of-numbers](V2.73-a-second-week-of-numbers.md) | F134 — Umami on grownativeplants.ca, because Cloudflare's free tier only shows the past 24 hours | Shipped. Read as "add the tool that keeps history", **not** "replace Cloudflare": the two answer different questions (*is anybody reading this* vs *which pages, and is that growing*), and running both at once is how you prove the new counter is recording before dropping the old one, so that is allowed rather than an either/or flag. Umami is admitted on the same three terms V2.71 set for the renderer's single "no external request of any kind" exception — off by default, no cookie and no per-visitor identifier, disclosed in the footer of every page it appears on. The vocabulary moved out to `src/site_analytics.py`: `static_site_render.py` was at **721 of 800** permitted lines and one provider becoming two in place would have spent the headroom on a concern that is not rendering, and the seam was real regardless (three globals and two functions scattered among the page shells). The footer sentence is now **generated from what is actually configured** — a footer naming Cloudflare on a build carrying Umami is the exact class of quiet falsehood V2.71 deleted from the About page after twenty increments — and `sets no cookies` is pinned present in every combination by a test, because that phrase is the promise the exception rests on and prose is the half nobody re-reads. `--umami-src` is a flag rather than a constant on evidence, not taste: **`cloud.umami.is/script.js` is on the common blocklists**, so the number is a floor and not a total, and serving the script from grownativeplants.ca is a real later move that now costs one word in a build command. **Not verified here:** the container has no egress, so no beacon was watched arriving at any dashboard — everything up to the network is tested (both tags on all 1,680 pages, the disclosure, malformed values raising, a later build unable to inherit an earlier one's configuration) and the last mile is in the runbook |
| V2.72 | [the-tags-were-pointing-at-the-wrong-commit](V2.72-the-tags-were-pointing-at-the-wrong-commit.md) | A push that failed with "src refspec V2.71 matches more than one", and the much larger thing behind it | Cause fixed, history left for the author to run. The ambiguity itself was **local and self-inflicted** — recovering from a container restart with `git fetch origin V2.71` resolved to the *tag*, and once both refs existed locally git could not tell which was meant; `remote.origin.tagOpt --no-tags` clears it, and a bare fetch otherwise drags all 102 tags back every time. The first instinct was to delete the tags and **that was wrong**: each one anchors a GitHub Release and `github_releases.parse_release_version` reads `tag_name` off those releases, so deleting them breaks the in-app updater for every packaged build. The real fault was four missing words: `softprops/action-gh-release` creates the tag when it does not exist, and with no `target_commitish` GitHub anchors it to the **default branch** — so `main` not having moved since the V2.20 merge put **51 tags on one commit fifty releases old, and 99 of 101 on the wrong commit with zero correct**. It survived fifty releases because nothing visible broke: the `.dmg`/`.exe` are uploaded assets and the updater reads `tag_name`, so only the "Source code" links lied. Fixed in **both** workflows, because whichever finishes first creates the tag. The retroactive half is `scripts/retag_releases.py` (dry run by default, backs up and prints its own rollback) and **could not be run from the session** — this credential cannot write tags at all, force-move and fresh-create both 403 while branch pushes succeed, with no proxy denial recorded. Two oddities left alone and reported rather than guessed at: `V2.0`/`V2.1` are tags with no branch beside the padded `V2.00`/`V2.10`, and the V2.47 branch tip is a commit titled "V2.49 follow-up". |
| V2.71 | [fewer-words-and-a-way-into-the-animals](V2.71-fewer-words-and-a-way-into-the-animals.md) | Eight copy edits from reading the published site, filtering on the wildlife index, and analytics | Shipped. The misaligned figures were not a number problem: grid cells stretch to the tallest in a row, so a one-line label put its number a line above a two-line label's and the bar read as four unrelated statistics. **Removing the About page's "What it does not contain" section deleted a claim that had been false for twenty increments** — it said the generic `medicinal` tag was "not published or searchable here", which stopped being true in V2.50 when the author ruled a horticultural category is not sourced traditional knowledge; `WITHHELD_ROLES` has been `()` since, and the landing page carries a "Medicinal 108" chip. Nothing about P12 changed with the section: the one-line statement is in the footer of all 2,064 pages with a test behind it, and the `notes` withholding lives in code, not prose. The wildlife ask paid for itself twice: **58 credited, openly-licensed animal photographs had been sitting in `fauna` unpublished** because the wildlife model had no photo field, and `photo_credit()` already refused any row without attribution, so publishing them was one line and the no-credit-no-photo rule came free. `browse.js` now drives both indexes — two hardcoded assumptions came out (a `plants/<slug>/` card lookup, and the word "plants" in the running count). A second unseen thing on the same page: its prose block was keyed on `animal["notes"]` and **`fauna` has no `notes` column**, so it had read `None` on every row since the model was written and had never once rendered, while `description` — all 1,156 rows of tongue length, nesting habit and forage genera — went unpublished. Analytics is built as an *exception* to the module's "no external request of any kind": opt-in, Cloudflare (no cookie, no per-visitor id), disclosed in the footer, and a malformed token raises rather than being pasted into 2,000 pages. Found while rebuilding: a container restart empties the image cache, and the first build would have published 321 hotlinks to iNaturalist — the species bytes were recoverable from the `gh-pages` branch with no network at all. |
| V2.70 | [the-website-gets-its-own-name](V2.70-the-website-gets-its-own-name.md) | Put GrowNativePlants on the website everywhere Site & Pattern was, now that the domain is registered | Shipped; written up late, at the start of V2.71. **Two names rather than a rename**: the app's name says what it does to somebody who has decided to design something, the site's says what a search-engine visitor wanted before they had a word for it. One `SITE_NAME` constant in the module every page's shell already passes through; `APP_NAME`, the QSettings keys, the database path and the frozen API symbols untouched, same reasoning V1.69 used. The app is named outright on the About page on the author's call. The part that would have broken silently: **GitHub Pages keeps the custom domain in a `CNAME` file at the root of the published branch and the publish replaces that branch wholesale**, so a domain typed into the settings survives until the next rebuild and then 404s with no error anywhere. Derived from `--base-url` rather than given its own flag, because two settings that must agree and are set separately do not stay in agreement. |
| V2.69 | [three-levels-you-can-click](V2.69-three-levels-you-can-click.md) | Fix the page URLs that move on every reseed, and make all three ecoregion levels selectable on the website map | Shipped. **The URL bug had its reasoning right and its field wrong**: `_unique_slugs` broke a name collision by suffixing the row id and said in its own docstring that this "keeps the URL stable across rebuilds" - while row ids are the one thing a reseed does not preserve, which CLAUDE.md states three times and `db/photos.py` keys on `scientific_name` to avoid. It hid because the id only shows in a *collided* slug, four pages out of 1,570; the V2.68 publish retired all four addresses silently. The test that should have caught it was called `test_colliding_names_keep_separate_stable_urls` and asserted the id-suffixed form. **Drill-down beat three side-by-side maps** because the reader's question is sequential, 24 regions do not fit on one map (that crowding is what produced the abbreviation shipping "Parkland" as a region name), and only a drill-down shows containment. Then the measurement killed the tidy design: **Alberta's subregions are not a third tier**, they are a parallel classification - 12 of 21 sit >=90% in one ecoregion but Montane is 42% of Northern Continental Divide across six, and Central Mixedwood 31% across nine. The first cut had filed Montane under Aspen Parkland (6% of it) because `sorted(parents)[0]` is alphabetical, and **the identical shortcut appeared independently in three modules within an hour** - the page model, the palette and the display name, the last of which printed "Dry Mixedgrass (Cypress Upland)" in a public legend about a 2% overlap. Shares are now measured in the pipeline and shipped as `sub_share`. Three more found only by rendering the pages: a legend naming 24 colours under a map of 6, a subregion focus map drawing 13 subregions, and labels colliding at a third of a column wide. |
| V2.68 | [twenty-four-is-too-many-for-one-list](V2.68-twenty-four-is-too-many-for-one-list.md) | The two decisions V2.67 left open: a filter dropdown that can hold 24 regions, and what happens to the heuristic `ab_ecoregion` tags | Shipped. The two asks look independent and are not — **the second is only survivable because of the first.** Moving a tag when its target has split offered two bad options: fan it out (nine assertions from one record, P9 through the back door) or drop it. The three levels add the missing one — keep the claim at the level its evidence actually supported — and that value was not representable until the author asked for the hierarchy. The mapping is **measured, not decided**: the old file is still in git, so each old polygon is intersected with the surveyed layer in an equal-area projection. It had to be, because the V2.67 plan wrote the name-based mapping down a day earlier and **got one of its four "clean renames" right**. Three of the six old regions turned out **misplaced rather than coarse** — `subalpine_montane` was drawn east of the mountains it is named after and its best overlap is 19%; `aspen_parkland` survived as a *key* and looked like the one safe rename in the set, but its polygon matches the real parkland only 40% and sits a third inside Boreal Transition. For those the ecozone does not rescue the tag, it disguises the error, so they are cleared: 431 → **384 species keep a tag, 47 lose their last one**. The brief's own warning landed one namespace further along than anyone was watching — "Athabasca Plain" is an ELC ecoregion in Saskatchewan **and** an Alberta subregion, the pipeline joined the frameworks spatially to avoid exactly that, and the tree then slugged both to `athabasca_plain`, so Mid-Boreal Uplands' lineage reached the Boreal Shield. Matching runs **both ways along a lineage** on purpose: a plant tagged only at the ecozone is unknown at the ecoregion, not absent, and match-exactly would have made the 304 newly `zone_prairies` species answer no query at all. 21 Alberta subregions become selectable for the first time — they were in the data since adoption and nothing had ever offered them. The guard fired again and got the extraction rather than a bigger number. |
| V2.67 | [the-app-learns-the-real-regions](V2.67-the-app-learns-the-real-regions.md) | Adopt the validated 24-region ELC layer into the app itself | Shipped — the 0.9 MB export landed mid-increment and the layer is in the app; the two decisions it deliberately left open (what happens to the heuristic tags, and whether 24 options belong in one dropdown) are V2.68. Adoption is smaller than it looks because V2.38 made **the polygon file the vocabulary** - *"adding a region means adding a polygon and nothing else"* - so the filter, the validator, site detection and the range tags all follow from replacing `data/ecoregions_canada.geojson`. A principle adopted for an unrelated reason paid off years later. Found first, by reading the consumer before replacing its input: **`lookup_ecoregions` skipped MultiPolygon entirely**, testing `type != "Polygon"` and moving on. Survivable while the six shapes were single rings; every published ecoregion layer is full of MultiPolygons, and skipping them does not raise - it answers *you are in no ecoregion at all*, which reads to a user as "we do not cover your area" rather than as a bug. Four tests now pin it. Also landed: `adopt.py` (its own command, never a side effect of `run`; `where` hints derived from each polygon's centroid so twenty-four of them cannot disagree with the geometry) and a palette bridge so the website colours `mid_boreal_uplands` correctly with no second edit. **The part that is not mechanical**: `plant_ecoregions.json` holds GBIF-derived ranges for 427 species keyed to the old six, of which exactly one key survives. They must be **re-derived, not translated** - fanning one `boreal_mixedwood` record across nine Boreal Plains ecoregions asserts nine occurrences where the evidence supports one, which is P9 failing through the back door, and is the same mistake the V2.38 runbook caught when throttled GBIF requests were being recorded as "grows nowhere". |
| V2.66 | [the-map-under-the-map](V2.66-the-map-under-the-map.md) | The ecoregion map, which the author called sloppy, against a brief diagnosing two prior attempts | Shipped, **in two halves, because only one was blocked.** The polygons need the National Ecological Framework and the Alberta subregions; both hosts refuse this session's egress proxy, and per the brief nothing was substituted. The **basemap and the palette needed neither**, and screenshot A in the brief is not a discarded draft — it is what the site serves today, at 700px on `/map/` and 420px on all 432 species pages. So those shipped now. The basemap was worse than the polygons and nobody had looked: **Saskatchewan and Manitoba were five-vertex rectangles**, the AB/BC border was a straight line where the real one follows the continental divide, and there was **no water layer at all** — the missing Peace and Athabasca are not a styling omission. Natural Earth 1:10m via `tools/ecoregions/basemap.py`; Alberta 15 vertices → **187**. Projection went to **Albers equal-area conic**; the brief's `ESRI:102001` used verbatim came out **tilted twelve degrees**, since Canada Albers centres 14° east of this window, so it is re-centred and the GeoPackage keeps the standard CRS. The palette **failed every check** a colour-vision validator has — the boreal teal and foothills olive ΔE 7.7 apart *with full colour vision*. Lightness now carries woody cover, which fixed five pairs of six; Aspen Parkland against Moist Mixed Grassland is green-against-orange on the red-green axis and collapses to **ΔE 4.3** under deuteranopia however lightness is arranged. A 260k-iteration OKLCH search found no solution holding the convention, and every solution without it was a cyan boreal and a magenta Rockies. So the brief's one-off Cypress hatch became a **rule**: two regions sharing a border reach CVD ΔE 8 or one is hatched, enforced in both directions against whatever polygons ship — an *unearned* hatch fails too. Adjacency, not all pairs: 11 of 15 possible pairs actually touch. The new tests immediately caught something unplanned — the pale gold came within **ΔE 7.4 of the not-recorded grey**, so three occurrence records looked like none. Also found: the two shipped grassland polygons **self-intersect**, failing the brief's own topology check in production. `tools/ecoregions/` is the six-stage pipeline; 1, 2 and 5 run here, and `render --from-shipped` exercises the whole cartographic path while stamping PLACEHOLDER on itself. `data/ecoregions_canada.geojson` **untouched** — it decides real garden recommendations, and `export --adopt` prints that checklist rather than doing it. 3195 tests, no failures. |
| V2.65 | [three-numbers-that-were-lying](V2.65-three-numbers-that-were-lying.md) | F131 + F120 + F129 — three places the app already held the information and reported something else | Shipped. Schema **v71**. **F131**: the ingest gate refused 700 `larval_host` candidates for not coming from GloBI's explicit `hostOf` verb — a rule that earned its place by stopping 23 false Monarch hosts. But unspecific is not false, and F128's observations file (on disk since V2.62) carries the life stage. **44 of the 700 have an observed larva on that exact plant**, and the specialists prove the method rather than merely add rows: *Schinia florida* on Evening Primrose, *Cycnia tenera* on Spreading Dogbane, *Chlosyne harrisii* on Flat-topped White Aster. 26 edges written, every one with a named museum-specimen study; `larval_host` 320 → **346**. A **pupa does not count** (larvae wander off the host to pupate — costs one real bagworm edge and keeps the rule true) and the adult → nectar reroute is **Lepidoptera-only**, because an adult beetle on a leaf is chewing it and routing that to nectar would be the Monarch bug in a new costume. No Monarch reached a non-milkweed. Knock-on nobody planned: V2.64's orphan filter withheld 18 admitted animals whose every edge was dropped downstream, and **nine gained one here**, so their fauna and attribute rows were written — two guards written for another increment caught it. **F120**: the score reads use tags, the citations live in edges, and nothing kept them in agreement — the app held a sourced host record for Chokecherry while telling the user their chokecherry planting has no host plants. V2.53 measured 48 and left it deliberately, since the fix moves scores. The first pass here proposed 101 species and was **wrong**: reading the relationship alone let a bumblebee, a grasshopper, a gall midge, a horntail and a **deer mouse** vote. 107 of 346 `larval_host` edges are not lepidoptera. `_TAG_BACKED_BY_EDGE` now names the taxon that has to be at the far end and the gate and fixer share it, cutting 117 to **80 real corrections** — `host_plant` 30 → 95, `bird_food` 78 → 104, worked example **+7 points**. Additive only: absence of an edge is absence of evidence. **F129**: the five vegetation layers are the permaculture *forest-garden* stack, so a prairie was capped at 6 of 15 by a component measuring how woodland-like a design is. The denominator is now the **reference community's own layers** — a prairie planting on prairie goes 6.0 → **10.0 / 15**, and the same planting in Aspen Parkland **drops to 3.8**, which is the point: a measurement that could only raise the number would be a compliment. Reuses `reference_fidelity`'s layer judgement rather than copying it. Also logged: a 27,495-line diff for 80 edits, because the fixer wrote `indent=1` into `indent=2` files — and the *first fix for that* still produced 27,493, because it read the file's format inside `open(path, "w")`, which had already truncated it. |
| V2.64 | [the-long-thin-end-of-the-list](V2.64-the-long-thin-end-of-the-list.md) | F130 — the 980 animals V2.63's top-200 review held | Shipped. Fauna 327 → **1,147**, edges 5,487 → **7,714**, coverage 293 → **302 of 437**; bibliography 46 → **114** works. **The tail is not more of the same, which is the whole plan.** Its median species carries two edges and 457 carry exactly one, so the concentration argument that bounded V2.63 has nothing left to bound — it is read whole or not at all. And the relationships change kind: `larval_host` goes from a tenth of the edges to nearly a quarter, and the animals are leaf miners, gall formers, seedhead flies and aphids rather than pollinators. That is the food web underneath the flowers and it is well evidenced — a mine or a gall is recorded by finding it *on* the plant — but it makes the per-animal question a second one: not only is it native, but **is this a relationship where the plant gives something**. Ambush bugs, crab spiders, robber flies, six genera of dragonflies and every arachnid are held on that ground; green lacewings are admitted on it, because the adult really does eat pollen. **Where the introduced species were hiding**: not exotic bumblebees but the everyday European insects of a settled landscape — carpet beetles, cluster flies, the Meadow Spittlebug, five genera of weevils, a Chinese Mantis sold as a garden beneficial, and two deliberate biocontrol releases. None flagged by GBIF; all with real Alberta records. **39 reject, 103 hold, 838 include.** Four superseded names would each have become a second row for an animal already admitted, and 42 trinomials made the hand-written rejects a rule instead — placed *above* the genus lookup, since most belong to admitted genera. The suite caught the real bug immediately: the first apply wrote **20 animals connected to nothing**, admitted honestly and then stripped of every edge downstream by the larval-host gate. `writable()` now asks the real ingest gates, 18 moths go unwritten, orphans 20 → 2. |
| V2.63 | [the-honeybee-at-the-top-of-the-list](V2.63-the-honeybee-at-the-top-of-the-list.md) | F127 — name and admit the animals holding 6,231 edges, after the author chose "scientific name where there is no common one" | Shipped. Fauna 167 → **327**, edges 2,813 → **5,487**, coverage 275 → **293 of 437** — against 361 edges over 99 species three increments ago. The reason it is a fifth hand review rather than a fifth filter is the top of the surviving list: **Apis mellifera, number one, 133 edges.** V2.62's review had only seen species GBIF *flagged*, and the strict Canada filter left the honeybee `unstated`, so it was never a candidate — it would have entered as a native Alberta bee. Beside it, a **Central Asian bumblebee with 1,125 Saskatchewan records**, which is a data error no threshold separates from a range. Four automated nativity signals have now failed against three hand reviews that worked. Bounded by concentration: the top 200 carry 57% of the edges, so 200 were read — **159 include, 17 hold, 24 reject**, the rejects taking 747 edges. Structured as **genus defaults plus species overrides**, because that is the shape of the knowledge: the genus is placeable where the species is not, and a wrong genus rule is wrong visibly in one line. Two bugs caught by the guards within minutes of writing them. `_shortlist()` recomputed "top 200 not yet in the catalogue", so writing 159 slid the window and pulled in **42 unread species including two introduced ones** — the list is pinned now. Pinning then exposed *Polistes fuscatus* falling silently to "not reviewed" for want of a genus rule, inside a list whose whole claim is that it was read. On naming: 56 of the 159 have an accepted English name and **103 keep their binomial**, which for a solitary bee is not a compromise but the name. |
| V2.62 | [twenty-four-species-read-by-hand](V2.62-twenty-four-species-read-by-hand.md) | Apply F128's citations, review the species GBIF called introduced, and bump the schema for the catalogue's first two photographs | Shipped. **2,406 of 2,452 GloBI edges now cite a named study, specimen collection or dataset**; 46 still say only "an aggregator has it", and the bibliography went 13 → 46 works. The observation fetch returned a citation on all 13,665 edges and — against the single-plant probe's 6% — **41% life stages**, a reminder that a probe percentage from one sample is not a corpus. Body part held at 0%, confirming the negative. GloBI calls four different things a "study" and each needed handling: real papers, portal URLs, GBIF dataset UUIDs and `urn:catalog:` specimen numbers. **Collapsing is the trick** — 1,357 distinct specimen URLs are 8 institutions and 9 URNs are one bee collection, so keyed per record the bibliography would have grown an entry per pinned beetle. Two source defects had to be handled rather than assumed away: one "citation" is `http://iNaturalist.org is a place where you can record…`, a URL glued to prose, which made the whole sentence a hostname and then a key. `data_quality` had already anticipated the shape of this — its `NOT_A_WORK` exemption exists because requiring an author "would force somebody to invent one" — so `specimen_record` and `dataset` join it, a pinned beetle having a collector and no author. **The introduced review** replaced two failed attempts to make GBIF answer the nativity question: the occurrence facet returns nothing on any species, and the checklist distributions contradict themselves — strict filtering fixed the Summer Tanager, made the Red-eyed Vireo *worse*, and lost *Apis mellifera*. 24 candidates out of 3,064 is a number a person can read, so it was read: **19 introduced, 5 native, 1 honest `unsure` that fails closed**. GBIF was right 19 times; **every clear false positive is a bird**, and birds already had a human verdict, which is why none of it reached the seed data. The reviewed call now beats GBIF both ways — but a corrected native still has to clear the occurrence floor. Held edges stand at 6,231 on 1,194 animals, and the blocker is no longer nativity but `common_name NOT NULL` against a queue of unnamed solitary bees. |
| V2.61 | [a-count-instead-of-a-claim](V2.61-a-count-instead-of-a-claim.md) · [runbook](V2.61-runbook.md) | *"Proper citations for each relationship, and only flora and fauna native to AB and SK"* | Machinery and gates shipped; the two fetches need egress and are handed over in the runbook. Measuring first changed both halves. **The flora was already done** — all 432 rows carry `native_provinces`, 356 of them `AB,SK` — so there is nothing to remove, and the five cultivated rows are a food-forest feature left alone on purpose. **The fauna is cleaner than expected and entirely unsourced**: no honeybee, no Cabbage White, *Eristalis tenax* correctly flagged introduced — but **142 of 167 rows assert nativity as a boolean nobody sourced**, with no province data at all, and 2,898 held animals need the same call made 2,898 times. GBIF answers it mechanically at one request per species per province (`limit=0` returns the count, the `establishmentMeans` facet rides along free), reusing the ecoregion seeder's throttle and its *a failure is not an absence* rule rather than retyping them. The load-bearing distinction is that **occurrence is not nativity** — a starling has tens of thousands of Alberta records — so presence, origin and verdict stay three fields, `unstated` is the common and honest answer, and `introduced` is refused however many records back it. The gate deliberately does not re-litigate animals already in the catalogue, whose nativity a person decided. Two self-corrections: the occurrence floor was picked as 5 and claimed to match `ecoregion_ranges`, which it did not (`MIN_RECORDS` is 3), so it is now imported rather than chosen; and the citation columns are read through an **alias list** rather than one guessed name, because V2.59 hard-coded `study_citation` — which does not exist — and the fixture repeated the guess, so the suite passed while every edge arrived uncited. `--probe` now prints every spelling with its population percentage and says outright when a run is not worth making. |
| V2.60 | [a-nectaring-sparrow](V2.60-a-nectaring-sparrow.md) | F124 (the vegetation-layer map, which missed 311 of 437 species) + F127a (the 67 birds F125 left held) | Shipped. Both are the app claiming what its data does not support. **F124**: `PLANT_TYPE_TO_LAYER` knew six of the catalogue's eleven types, so `wildflower` — 210 species, the largest group by a factor of four — plus grasses, sedges, rushes, aquatics and the one fern counted as no vegetation layer, and a prairie meadow scored 0 of 15 on a component it plainly satisfies. **The number quoted when asking for the decision was wrong**: mapping all five to `herbaceous` gains *one* layer, so the meadow reaches 3 of 15, not the 9–12 promised. Reading `mature_height_meters` gets short forbs into groundcover and reaches 6, with the 0.30 m boundary read off the catalogue's own `groundcover` type rather than invented. The residual is bigger than the fix and is raised rather than taken (**F129**): the five layers are the permaculture *forest-garden* stack, so a grassland is structurally capped at 6 of 15 by a component measuring how woodland-like it is. **F127a**: 67 birds decided one at a time — 37 include, 5 hold, 24 reject, with the European Starling rejected precisely *because* its chokecherry record is true. Three bugs behind them, the first being V2.59's Monarch error wearing new clothes: `eatenBy` had been read as `fruit_food` for any bird, and 10 of the 33 target plants bear no fruit at all — a crossbill on a tamarack eats seeds, a sapsucker on a birch drinks sap, and GloBI filed three *flower visits* for a White-crowned Sparrow, which does not nectar. Then the first cut of the fix binned 62 good edges belonging to the 24 birds the catalogue already held, because a gate that silently drops what it does not recognise is worse than no gate. And `Picoides pubescens` would have become a second Downy Woodpecker beside `Dryobates pubescens`. Since the routing arrived one increment after the apply it should have governed, `--refit` re-derives already-written rows — 20 re-routed, 34 removed. 12 included birds are deliberately **not written**: every one of their records drops, and the orphan-fauna guard caught it at 14. 115 candidate bird edges became 34 real ones, which is the honest result. |
| V2.59 | [the-edges-were-real-all-along](V2.59-the-edges-were-real-all-along.md) | F125 — actually source the plant↔animal relationships V2.58 had only made the absence of visible | Shipped. 361 edges → **2,800**, coverage 99 of 437 species → **271**; the author's 16-plant design goes from 1 species with records to 12. Split into a fetcher that runs where there is internet and a gated ingester that runs here and **reports by default**, which is the design the egress policy forced and the right one anyway. Three faults, each caught by something other than reasoning. The fetch 500'd on all 55 plants and the handler *swallowed the response body*, so it said "failed" 55 times and never why — fixed with a `--probe` that reports which query form works and what percentage of rows populate each column, and the author's one live run settled three API facts guessing had got wrong. Then all 14,111 candidates were binned for "no reporting study": GloBI returns `study_title`, not `study_citation` — and **the test fixture encoded the same wrong guess as the code**, so it passed while both were wrong. Worst of all, the first apply put 23 Monarch caterpillars on goldenrod, aster and sunflower, because "life stage unrecorded → larval host" inverts for butterflies, where adult nectaring is most of what GloBI holds; `test_fauna.test_monarch_only_hosts_on_milkweed`, written years earlier, caught it, and `larval_host` now needs the explicit `hostOf` verb (drops 107 of 120, and that is the right trade). Applying the data then broke a food-web test that was **telling the truth**: a documented hummingbird nectar record on milkweed made a milkweed-only design report a complete Tallamy chain, on the one caterpillar birds famously refuse to eat — so the chain's bird count now excludes flower visits. 10,937 further edges are held, blocked on 2,872 animals whose nativity nobody has curated (**F127**), and per-study citations wait on an observations-mode re-fetch (**F128**). |
| V2.58 | [the-app-was-lying-about-birds](V2.58-the-app-was-lying-about-birds.md) | Eight items of author feedback on V2.54–V2.57, the first time all four increments were tested together | Shipped. The headline report — *"16 natives and no animal interactions… did something break"* — was **two** failures, and the instinct was right even though nothing had broken. **A false claim**: `has_birds` fell back to the `bird_food` tag, so a design with zero bird records reported `birds: True, n_birds: 0, complete: True` and the panel printed "supports the birds that eat them". That is the V2.53 contradiction running backwards — that increment fixed tags *denying* documented edges and left tags *claiming* unrecorded ones, while its docstring called the dict "edge-derived", which it never was. **And invisible coverage**: `plant_fauna` covers 99 of 437 species, and the app never said so, so a thin catalogue read as an empty ecology. Both fixed without moving a score, since `food_web` is un-summed. Genus inference was measured (rescues 59 of 338) and rejected in favour of real sourcing, **F125**. Also: OSM road data now *measures* the frontage instead of assuming south — which took three tries, because the first version named the wrong edge (nearest endpoint, not closest point, on a road that runs past the lot) and `_ring_lnglat` silently ate two-node ways. Tooltip legibility, the undo tooltip, the clipped "Take into Design" button and substitution-on-the-map all fixed; before/after deliberately **not** polished, since the author judged the idea itself weak (**F126**). |
| V2.57 | [the-last-centimetre](V2.57-the-last-centimetre.md) | Group D — F92 (an order file a nursery accepts) and F91 (ecological substitution), the two cheap ends of the professional workflow | Shipped. **F92** is the buy list as a CSV you attach to an email, numbered to match the planting drawing. Two things the survey settled before any code: "grouped by supplier" is **not derivable** — there is no plant-to-nursery stock data anywhere in the app — so it groups by the `availability_class` enum the schema created for exactly this and names the real nurseries near the pin; and XLSX was skipped rather than adding `openpyxl` to a frozen build. **F91** redefines "similar plants" as *ecologically equivalent* — same group, overlapping envelope, overlapping supported fauna — and reports the trade in full, because a list that only sells the alternative asks to be trusted on a judgement it has not shown. Thinly-recorded plants are ranked rather than excluded, since demanding 25% fauna retention from a plant with one recorded relationship punishes the user for a gap in the data. Three bugs found: `sun_requirement` is comma-separated and was compared as a scalar (**76 of 80 candidate grasses wrongly rejected**), `hardiness_zone_min` can be `'4?'` and crashed `int()`, and **F124** — `PLANT_TYPE_TO_LAYER` covers 6 of the catalogue's 11 plant types, so a prairie meadow of wildflowers, grasses and sedges scores **0 of 15** on layer diversity. The last was measured and left to the author, since fixing it raises every affected design's score. |
| V2.56 | [the-argument-never-made](V2.56-the-argument-never-made.md) | Group B — F76 (before / after / in five years) and F75 (cues to care), the argument the app has never made | Shipped. The design side had not had an increment since V2.42 and this was the backlog's own #1: the app argues ecological value superbly, and nobody converts a lawn on those grounds. **F76** assembles ingredients that had sat unjoined for three releases — one camera across all panels, because a viewpoint change reads as a change in the design, and years taken from `snapshot_timeline`'s vocabulary rather than from the number 5. The two "before" cases are captioned differently on purpose. **F75** is six Nassauer cues; the frontage cannot be derived (no road data exists anywhere in the app) so it reuses the `sidewalk` camera's south-edge assumption and **names it in the one line that depends on it** — a test asserts the other five never mention it, which is what makes a wrong guess cost one line rather than the feature. Three findings: a corrupt site photo used to drop the before-panel silently; **F123**, that `still_pixmap` is never passed by `_on_export_pdf` so F69's page has been unreachable since it shipped; and a test of my own that segfaulted the suite by pumping a Qt event loop mid-run, found with `-X faulthandler` and replaced by a scheduler the test controls. |
| V2.55 | [say-what-the-number-means](V2.55-say-what-the-number-means.md) | Group E, the Learn side — narrowed to its two buildable rows, F122 (the Site Info glossary) and F104 (sandbox undo + the graduation path) | Shipped. Group E is seven items and three are blocked on things that do not exist — mascot art, a template conversation, unfilled photo slots — so the increment took the two the backlog itself flags. **F122**: the count was wrong in the direction that matters, **15 of 20** metrics unexplained rather than "~12", and the deeper problem was that all five existing tooltips sat on the *value* label, so hovering the word you are stuck on showed nothing while hovering the number explained itself. One Qt-free glossary now feeds both, `climate.zone_description()` gets the caller it never had, and the zone's text is refreshed where the value is measured because set at build time it would name the wrong winter. **F104**: `scene3d_edit_flow`'s own comparison table already said `undo: none` for the sandbox; **Reset is undoable too**, which is the point rather than a nicety. Graduation turned out nearly free — `main.py` already builds MainWindow in Learn mode and never shows it, *"so that stepping into Design later is instant"* — and wiring it caught a bug worth naming: without carrying `site_config` across, graduating would open at the right address with the whole Site Info panel blank **and overwrite the site data the user already had**. The window was at 450 of 450 lines, so the edit bar moved into `reference_edit_flow`; the module that owns what the buttons do now owns what they look like. |
| V2.54 | [hold-still](V2.54-hold-still.md) | F121 — let the user pause and slow the 3D preview's ambient wildlife, so a creature can actually be looked at | Shipped. The ask sounds like a multiplier and is not: `animateWildlife` runs on **two** clocks, `dt` for travel and **absolute `t` for the wingbeat**, plus the bob, wobble, hop and `beatGain`. Scaling `dt` alone — the implementation anyone reaches for first — gives a bird hovering motionless in mid-air with its wings beating at full speed. Fixed with one wildlife-local clock that both read, so the whole body of the function is unchanged and one multiplier moves everything coherently; the pre-existing 50 ms clamp meant resume-after-pause was free, and it closed a latent bug in passing, since a frame hitch used to clamp `dt` but not `t` and let the wingbeat drift ahead of the travel. Two things found before they became bug reports: **Space was already bound to "ascend"** by `_BEE_MOVE_CODES`, so the shortcut stands down in first-person modes; and the control is a HUD chip rather than an **eleventh** button on a toolbar that is already the literal F89 complaint at 914 of 950 permitted lines — which also means no `window.perma*` hook and so no bridge-contract exemption to keep honest. The permanent halving of bird speed in `_WILD_MOVE.perch` is left alone but is now revisitable, since ¼× is the instrument for judging it. |
| V2.53 | [the-confidence-block](V2.53-the-confidence-block.md) | Group A of the new backlog — F8, F12, F13, F14, F28 — the five cards the roadmap called one job and skipped twelve times | Shipped, over one shared vocabulary so five cards could not invent five scales. Two cards were not what they said. **F8**: the language audit found the prose mostly honest already (five uses of "will", all harmless) and the real failure elsewhere — `design_critic` asserted *absences* as facts off a use-tag score, while the app holds cited edges that disagree for **48 species**, Chokecherry and Balsam Poplar among them. The contradiction sits inside a single dictionary: `components.host.score` is tag-derived and can read 0 while `food_web.caterpillars` reads True. **F14**: the card proposed bucketing `score_cell_for_plant`, which would have printed confidence about nothing, because `build_cell_env_map` fills every unread grid with a neutral 0.5 — so on a site with no DEM every plant bands identically. Built off the georeferenced occurrence records instead, where "no record" reports as unknown rather than as unlikely, because under-collected and absent are indistinguishable below the floor. The 48 tags were measured and **deliberately not corrected**: it moves every affected design's score, which the stability rule makes the author's call, so it became a tracked gate warning and backlog row F120. |
| V2.52 | [the-backlog-enumerated](V2.52-the-backlog-enumerated.md) | Review `ROADMAP_NEXT.md` and produce one list of every feature not yet built, to choose from | Shipped as `docs/BACKLOG.md` — 41 features, 6 data jobs, 4 legacy items, every status checked against the tree rather than against another document. Six disagreed: F77 shipped in V2.33 and this document had *predicted* it would ("a sidewalk preset that is F77 for free") and never struck the card; F62 shipped renumbered as F65 and was still being deferred; F60 was absorbed by F64; F12's formatter shipped in V2.42 but reaches no design-side surface; F73 stopped being a schema bump when `taken_on` landed; F20 was delivered by F42's falling hour bands. F85 and F106 turned out to be one feature described twice in the same words. And the ID collided a third time: the pointer read "next free F111" after F111 and F112 had shipped, F90 was both the shipped plant directory and unbuilt design variants, and V2.46 and V2.47 both took F108–F110. The F90 collision had been **caught in the V2.43 plan, written into its docs-to-update list, and not acted on** — which is the argument for replacing the next-free-ID sentence with a ledger table, since a sentence is a step you can skip a release without noticing. |
| V2.51 | [colour-per-region](V2.51-colour-per-region.md) | The remaining four follow-up items: a colour per ecoregion and a closer map, a way to actually start on the flower-colour backlog, the native-nursery rule narrowed, and how to publish the site | Shipped. The ask was one sentence and the design problem was not: the fill was already carrying GBIF confidence, so hue took region identity and lightness took confidence rather than one replacing the other. Drawing the regions in colour then exposed three bugs that a flat green had hidden: a near-square map letterboxed into a landscape frame at half size, "Montane" printed in British Columbia, and BC drawn as a hole because Alberta's border leaves the divide at 54N and the ring was missing that corner. And a fourth that was not cosmetic, a real coverage hole near Rocky Mountain House that V2.49's sweep passed straight over because it sampled at 0.2 degrees and the hole is 0.1 wide. V2.49's nursery fix had made the filter match 432 of 432; a filter that matches everything is not a filter. The Peace River Parkland is still missing and is raised rather than taken, because adding it changes what real sites are recommended. |
| V2.50 | [merge-and-medicinal](V2.50-merge-and-medicinal.md) | Two of five follow-up items: merge the duplicate species rows under the author's chosen names, and publish the `medicinal` use tag | Shipped. The merge is field by field rather than winner-takes-all, because the two rows of each pair were authored at different times and each knew things the other did not. `edible_parts` looks like a comma-delimited set and is not: unioning shredded "roots (medicinal, not culinary)" into three tokens. The medicinal decision from V2.48 is reversed on the author's call; the withholding mechanism stays and is tested against a temporary value, because an empty tuple proves nothing. **Also the branch correction**: V2.48, V2.49 and V2.50 had all been committed onto the V2.47 branch, so the release branches the in-app updater looks for did not exist. |
| V2.49 | [a-map-of-somewhere](V2.49-a-map-of-somewhere.md) | *"The map is atrocious"*, plus nine specific corrections to the website's filters | Shipped. The ten placeholder rectangles became six real shapes with provincial borders and city dots, and because those polygons decide which plants a real property is offered, the authoring script self-checks 20 pinned lookups and sweeps for coverage holes before it writes. That sweep immediately found one: tracing adjacent regions separately had left a band east of Calgary belonging to no region, so regions are now cut from shared boundary lines. Two filter bugs were real. Toxicity ORed within the facet, so ticking a second safety box *widened* the result; facets now declare any/all and roles were quietly wrong the same way. And 81 grasses were filed as having showy flowers for the same reason the colour filter nearly filed them under yellow. Non-natives are excluded by matching the garden file rather than the native flag, because the flag gets Stiff Goldenrod and the duplicate Bee Balm backwards. |
| V2.48 | [the-colour-was-a-guess](V2.48-the-colour-was-a-guess.md) | Four reports on V2.47: flower colour sourced per genus ("you have blue and yellow columbine as red"), no colour filter in the plant tab, a website searchable on four axes out of sixty-eight columns, and a request for ecoregion maps, a higher-end look and no em dashes | Shipped. The colour bug was not introduced by V2.47, it was *revealed* by it: the column had always been a genus-level guess correctly labelled `estimated` in a field nothing displayed, and 32 genera shared one hex. With no flora reachable, 36 species were corrected against evidence that travels with them (Latin epithet, accepted common name) and the other 359 are now marked *not verified* rather than silently estimated. Five audit trips were left alone on purpose and carry their reasons, because a colour word in a name usually describes the berry. Drawing the range maps exposed a separate bug: three duplicated scientific names meant one row of each pair silently lost its GBIF ranges. 23 facets replaced 4; `medicinal` is excluded from the public role vocabulary for the same P12 reason the notes are. |
| V2.47 | [colour-and-a-public-catalogue](V2.47-colour-and-a-public-catalogue.md) | Two gaps a competitor read turned up: no flower-colour filter despite holding the hex since schema v31, and the F90 plant directory locked inside a desktop installer | Shipped. The data made the design: only 23 distinct hexes are in use, and the largest bucket (79 rows of `#cbbd80`) turned out to be *every* grass, sedge and rush — the wind-pollinated default, not a colour, so it got a bucket that says so rather than being filed under yellow. The site build swept all 439 species through `species_entry` and found a `ValueError` that had made one species' page unopenable in the desktop directory since F90. P12 forced a real decision: ~43 `notes` rows describe traditional medicinal use, and publishing to the open web is not the same act as showing it in a panel, so notes are withheld by default. The competitor itself was never reachable — egress policy — so the comparison rests on search indexing of their own pages. |
| V2.46 | [life-size-and-a-net-in-his-hand](V2.46-life-size-and-a-net-in-his-hand.md) | Five reports from watching V2.45's animals fly: a butterfly beat that is *"too fast and unnatural"*, creature sizes that are *"all wrong"* against the walker, no editing in the 3D preview, creatures passing through trees and buildings, and generic sprites | Shipped, except the sprite painting — the bench the author remembered (`scripts/tune_fauna.py`, bee band colours and all) already exists, and the part that does not is a wing-texture pipeline against GLBs that ship with no UVs by contract, so it is scoped rather than squeezed in. The scale error was 6–39×, and the author's own aside named the evidence: the calligrapher fly read "more correctly" because at 15 mm its wrongness happened to land closest to plausible. Two independent causes behind one flapping complaint — butterflies were using a *crow's* bout, and `beatGain` never stopped the wings at all, it shrank them to a 22% tremble at full rate. The guard fired for the third increment running and named its own seam again. |
| V2.45 | [wingbeats-that-come-from-the-animal](V2.45-wingbeats-that-come-from-the-animal.md) | Give birds, butterflies and bees real flight physics; put the left mouse button back on actions; record the Learn-side backlog in the standing roadmap | Shipped. Two faults, and fixing only the obvious one would have left the bug: the wingbeat constants were not hertz, *and* the body path was decoupled from the wings — that decoupling is the zipline. The bounding correction is derived (f/√d) rather than fudged, and applies only where the wings FOLD: the first cut corrected gliders too and overpredicted a hawk threefold. Birds were the only flying taxon with no morphology table at all. No network this session, so all 24 rows ship unverified and say so. |
| V2.44 | [the-sandbox-becomes-a-place-you-can-work](V2.44-the-sandbox-becomes-a-place-you-can-work.md) | Four things walking the V2.43 sandbox exposed: walk controls that did not match the 3D preview, no animation on any edit, a new plant appearing full-grown among its neighbours, and no split-screen map+3D | Shipped. The find: nothing anywhere carried a per-plant planting date, and `growth_scale_factor` treats year 0 as *mature* — so per-plant age had to be opt-in (`planted_year` absent = today's behaviour) or every saved design would have changed appearance silently. Nursery ages (tree 5, shrub 3) come straight from the author: nobody plants those from seed. The architecture guard fired for the second increment running and named its own extraction in advance. |
| V2.43 | [learn-and-design-two-doors](V2.43-learn-and-design-two-doors.md) | Split the boot screen into Learn and Design, make the reference landscapes editable, and start the species-discovery ledger — plus a designed backlog for the game side (flight physics, companion, challenges, the net, curriculum) | Shipped. The move that made it cheap: Learn mode carries a *real project* — the reference landscape — so `lesson_track`, `plant_impact`, `habitat_score`, `docent`, `field_study` and `chickadee_scenario` all bind to it with no rewrites, because every one of them already took `placed_plants` as a plain argument. Reverses V2.42's "no Learn/Design toggle" on the grounds that a door at boot is not the mid-session toggle that objection was about. |
| V2.37 | [user-feedback-easy-wins](V2.37-user-feedback-easy-wins.md) | Record the first outside tester's sixteen items; ship the ten that were cheap | All ten shipped. Three turned out to be features that already existed and could not be found — including PDF export, which had been raising `NameError` on every call for four minor versions behind a test that skipped. |
| V2.38 | [ecoregion-rebuild](V2.38-ecoregion-rebuild.md) | Second round of feedback: rebuild the ecoregion data properly, send feedback by email, fix three regressions | In progress. Both bugs and the sun/shade merge shipped; the ecoregion pipeline is built and tested, with its two network steps written up in [ecoregion-runbook](V2.38-ecoregion-runbook.md) for a machine with open egress. |
| V2.42 | [first-user-on-the-relationship-web](V2.42-first-user-on-the-relationship-web.md) | First user feedback on the F5 relationship web and F46 pull-a-plant: rings that meant nothing, unkeyed icons, no click-through, and a stale plant list | Shipped. Three of the four reports were features that *worked* and could not be read, which produced a new CLAUDE.md convention: hand back a click-path test plan at the end of every increment. Pull-a-plant was stale, not arbitrary — it only refreshed when the habitat-score button was pressed. Also explores, without building, the author's design-vs-education split. |
| V2.42 | [biological-data-review](V2.42-biological-data-review.md) | Audit the biological data: where the gaps are, and where the citations and sources are — flora↔fauna relationships especially | Review shipped ([DATA_AUDIT.md](../DATA_AUDIT.md)); the four app-changing parts scoped and held for the author. The suspicion was wrong in an interesting way: all 361 edges are properly cited, and not one citation is visible anywhere in the UI. On the author's push-back, coverage was measured too: it roughly doubles (22.6% → 51.3%) from host records already sitting unread in the shipped attribute files, and 24 of the orphan fauna turn out to be cuckoo bees the schema cannot express. Two existing docs were found making claims the code does not support. |
| V2.41 | [start-screen-and-directory](V2.41-start-screen-and-directory.md) | The start menu becomes a landing page with a third door, and F90 builds the room behind it: the catalogue as a browsable reference work with a species page | Shipped. The directory is almost entirely reuse — the list, the facets, the relationships layer and the quiz all existed. The find: `search_plants` takes thirty parameters and the design-side browser wires fourteen; sixteen filters had worked the whole time with nothing to press. Generate a design came off the start screen on the author's call. |
| V2.40 | [start-menu](V2.40-start-menu.md) | Promote the first-run welcome to a start menu: continue, open, and crash recovery as rows that appear only when they mean something — opened as its own window *before* the map | Shipped in two cuts. The first got the rows right and the moment wrong (a modal 150 ms after the map painted, which is a greeting over the app); the second moved the whole menu ahead of `MainWindow`, which is what the ask meant. Verifying that turned up three stacked process aborts that had been stopping the suite from ever printing a summary, and a real V2.40 regression hiding behind them. |
| V2.39 | [game-style-saves](V2.39-game-style-saves.md) | F87 — a saves folder, Save that stops asking, and an in-app list instead of the OS file dialog | Shipped. The autosave moved out of its `$HOME` dotfile in the same increment, with a copy-verify-unlink migration, because the one launch that needs it is the one after a crash. |
| V2.38 | [ecoregion-runbook](V2.38-ecoregion-runbook.md) | The two steps that cannot run in a cloud session — the CEC polygon download and the GBIF range derivation | Handed over. Not a plan so much as the other half of one; kept here because the reasoning behind the threshold and the two axes belongs with it. |
